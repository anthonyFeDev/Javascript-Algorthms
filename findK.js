
function findNumber(arr, k) {
    for (let integer of arr) {
        if (integer === k) {

            console.log(integer)
            return "YES";
        } else {
            return "NO";
        }
    };
}

console.log(findNumber(, k= 5));