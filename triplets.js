function triplets(t, d) {
    // Grab 3 random items out of the array
    let triplets = []
    let randomInt;
    for (let i = 0; i < d.length; i++) {
        if (d[i] + d[i] + d[i] < t) {
            add
        }
    }
}

console.log(triplets(t = 6, d = [3,1,2,3,4]));